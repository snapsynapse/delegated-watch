## Summary

## Why

## Checklist

- [ ] `npm test` passes
- [ ] `npm run validate` passes
- [ ] Every configured `eval:*` command passes
- [ ] `CHANGELOG.md` updated
- [ ] A change to a design invariant is recorded in `INTENT.md` first

## Data and privacy

- [ ] No prompt text, response text, customer data, private URL, secret, or transcript
      enters the repository or the schema
- [ ] Committed data remains scrubbed aggregates only
