# Delegated.watch INTENT
## What this is
A local-first record of the work delegated to AI models, aggregated to one scrubbed row per day per source. Extractors and captures read provider logs and usage APIs read-only, emit receipt JSONL, and a deterministic importer merges them into a single normalized JSON file that a static page renders. Token counts are the unit; delegated work is the subject.
## Why it exists
Usage evidence is scattered across products that never reconcile with each other: local transcripts, organization usage APIs, consumer chat exports, IDE extension stores, and local runner ledgers. Each tool shows its own slice and calls it the total. Nothing distinguishes no usage from no evidence.
The framing comes from Nate B. Jones' post, [Build a Token Burn Dashboard to Track What Your AI Actually Does](https://natesnewsletter.substack.com/p/token-burn-dashboard): token burn is a proxy for delegated intelligence, and measuring it is a learning loop rather than a leaderboard. See `ATTRIBUTION.md` for lineage.
## Design invariants
Non-negotiable. A change that breaks one of these is a change to this document first.
1. Unknown is never zero. A day with no recovered evidence is absent from the dataset. A zero means a measured zero. The UI renders the two differently.
2. Fidelity is declared per source entry, as `exact` or `estimated`. Placeholder (`sample`) data may not exist in committed data.
3. Count each inference request once. Where a request appears in provider, gateway, extension, and local logs, dedupe on a stable request id and prefer the most authoritative exact counter.
4. Raw logs never enter the repo. Normalize outside, commit only scrubbed aggregates. `evidence` describes a work family or review source, never prompt text, customer data, private URLs, secrets, or transcripts.
5. Cache and reasoning token treatment is explicit and consistent across every extractor. Cache reads are excluded from headline counts and preserved in receipt provenance.
6. A shared account or subscription quota does not imply shared local history. Coverage must state the machine, account, surface, and interval it represents.
7. Provider or model identity and surface are separate dimensions. An IDE extension is a surface; the model behind it is a provider.
8. Personal values live in `config/`, not in code. Timezone, window start, and account or machine aliases are configuration. Scripts read them; scripts do not contain them.
9. Extraction is idempotent and read-only against every source it touches.
10. Interrogate every input before trusting it. A read that fails must be classified, not swallowed: does not exist and cannot be read are different findings, and only the first is evidence of absence. Any check that cannot distinguish them is a defect, whether it lives in code, in a script, or in a status report.
## Scope boundaries
In scope: a person-scoped, token-denominated record of delegated AI work across providers, interaction surfaces, and machines, including local inference.
Out of scope:
- Cost modeling, pricing tables, budgets, and spend alerts. Tokens are the unit; dollars are a separate concern with its own staleness problems.
- A backend. The dashboard is a static page over one JSON file.
- Any scoreboard framing, or a chart that cannot change the next decision.
- Centralizing raw logs anywhere, including in this repo.
## Conformance philosophy
N/A because this repository is a product, not an open specification. It defines no conformance ladder for third parties. Its own claims are checked by the `eval:*` commands in `package.json`, which any clone can run.
## Admission criteria for changes
A change is admissible when it runs on canonical Node 24 LTS or the verified-compatible Node 26 line, `npm test` passes, `npm run validate` passes, and every configured `eval:*` command passes. A change to a design invariant above is recorded in this document before the code changes.
## Relationships to other PAICE standards
One adopted target. GuideCheck is a conformance target as of 2026-09-22; the rest remain non-binding.
- HardGuard25: not adopted. The dataset is token-denominated but publishes no budget or limit metadata for LLM consumers, so the adopt-when condition does not hold.
- Graceful Boundaries: not applicable. There is no API or MCP surface, so there is no agent-facing refusal path to format.
- GuideCheck: adopted at Level 3 (2026-09-22). The site serves `/.well-known/assistant-guide.txt` under the Human-Verifiable Assistant Guide profile 2.0.0, scoped to installing this repository and walking the importer gates against the shipped synthetic fixtures. The adopt-when condition held once the install path became something an agent would execute on a reader's machine: an install guide that reaches an assistant through HTML can carry text the human never sees, and the record this tool keeps is worth nothing if the tool arrives through an unreviewed instruction surface. Level 4 is prepared but not asserted on the page. The sidecar manifest at `/.well-known/assistant-guide-manifest.txt` pins the guide's bytes, the guide names it, and a DNS TXT record at `_assistant-guide.delegated.watch` carries the same hash with a TTL under 3600. The three landed in one change deliberately. A guide that declares `manifest-url` is evaluated against Level 4, so publishing the manifest before its anchor resolves makes a fetching verifier report the missing independent anchor as a blocking finding: the deployed guide would tell every assistant not to proceed. That is why the guide line, the manifest, and the TXT record are one change and never three. The manifest's `immutable-release-url` names `v0.2.0`, a release not yet cut; the profile permits a manifest to name the release that will contain the guide, and verifiers check reachability at verification time, so Level 4 does not verify until that tag exists. The reference CLI caps local evaluation at Level 3 and cannot establish an anchor, so Level 4 is confirmed by a fetching verifier or not at all, and this repository claims only what has been demonstrated. Every later guide edit rotates the guide hash, the manifest, and the TXT record together, in that order. `tests/assistant-guide.test.js` asserts the byte profile, the approval gates, and every `exec-sha256` pin on every CI job; a pinned script that changes fails the build rather than leaving stale provenance in place. The profile's `X-Content-Type-Options: nosniff` and `Strict-Transport-Security` response headers are SHOULDs this site cannot meet: GitHub Pages gives a project no control over response headers. Recorded here rather than worked around, and not counted as satisfied.
- EveryAILaw: not cited. The published surface carries synthetic demonstration data and processes no one's personal record, so no live AI-regulation obligation attaches to it.
- AI Posture: not published. Publication is opt-in and no assertion has been elected for this subject.
## Exceptions to Repo Standards
- Accessibility evidence is a source-contract test plus a dated scan, not a continuous axe gate (2026-09-22). `tests/ui-accessibility-contract.test.js` asserts the accessible names, landmark roles, and visible focus treatment of every keyboard-scrollable region, and runs on every CI job. It is cheap and blocking, but it only asserts what someone thought to add: a live axe-core 4.12.1 scan on 2026-09-22 found a scrollable `#drivers` region the contract had never covered. That finding is fixed and the contract now covers it. The scan is the evidence of record for that date; the contract test is the regression guard between scans. Rerun a scan when the page gains interactive state, and do not describe the contract test alone as a WCAG result.
- No `llms-full.txt` at the site root (2026-09-22). `docs/llms.txt` inlines what a reader needs about the site rather than only linking out, which is the comprehensiveness criterion the hygiene matrix makes the OPTIONAL carve-out conditional on.
- No `DefinedTerm` structured data on the home page (2026-09-22). The canonical-page pattern used across the sibling repositories emits `TechArticle` plus `DefinedTerm`, because those repositories publish open specifications and the block stakes the term a third party conforms to. The Conformance philosophy section above says this repository is a product and defines no such term, so the page emits `TechArticle`, `SoftwareApplication`, and `WebSite` instead. Adding `DefinedTerm` would assert membership in a defined-term set that the prose explicitly declines to claim.
## Changelog
- 2026-09-22: adopted GuideCheck at Level 3 and said so on the home page. The served `/.well-known/assistant-guide.txt` is the agent-facing install path; the home page carries the human one. Reversed the GuideCheck not-applicable entry recorded earlier the same day, and with it the claim that no PAICE standard is a conformance target. Rewrote the home page around the claim the project actually makes, which is one record across every provider, surface, and machine for as far back as the evidence survives, and linked the published real record at sam-rogers.com/ai-usage as the worked example beside the synthetic demo.
- 2026-09-22: separated the home page from the demonstration. `/` is now a built landing page describing the project (`src/landing.html` through `scripts/build-landing.js`) and the dashboard moved to `/demo/`, declared as `demo_path` in `config/site.json`. The dashboard's discovery metadata became route-aware: away from the site root it describes itself as a `WebPage` inside the site rather than claiming to be the site. `--self` candidate verification now excludes paths git ignores and does not track, which it previously reported as inventory extras. The byline cites 2026-06-22, the first commit of the private predecessor repository, as the publication date; this repository's own first commit is the release date.
- 2026-09-22: added the canonical-page surface (head metadata, WebSite and SoftwareApplication JSON-LD, favicon, web manifest, 404, sitemap) and reversed the sitemap exception recorded earlier the same day.
- 2026-09-22: added Conformance philosophy and Relationships sections to complete the nine-section template; recorded the accessibility-evidence and `llms-full.txt` exceptions from the repo-standards walk.
- 2026-09-22 (0.1.0): first public release of this candidate tree.
